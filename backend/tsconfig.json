{
  "compilerOptions": {
    "target": "es2020",                                 /* Target latest ECMAScript version supported by Node.js. */
    "module": "commonjs",                               /* CommonJS modules are the default for Node.js. */
    "rootDir": "./src",                                 /* Set the root directory for TypeScript source files. */
    "outDir": "./dist",                                 /* Specify the output directory for compiled JavaScript files. */
    "strict": true,                                     /* Enable strict type-checking options. */
    "esModuleInterop": true,                            /* Better compatibility with common libraries like Express. */
    "forceConsistentCasingInFileNames": true,           /* Disallow inconsistently-cased imports. */
    "skipLibCheck": true,                               /* Skip type checking of all declaration files (.d.ts). */
    "resolveJsonModule": true,                          /* Allows importing JSON files. */
    "moduleResolution": "node",                         /* Use Node.js-style module resolution. */
    "paths": {
      "@/*": ["./src/*"]                                  /* Alias for cleaner import paths. */
    }
  },
  "include": [
    "src/**/*.ts",                                      /* Include all TypeScript files in the src folder. */
    "src/**/*.d.ts"                                     /* Include any declaration files in the src folder. */
  ],
  "exclude": [
    "node_modules",                                     /* Exclude node_modules to speed up the build process. */
    "dist"                                              /* Exclude the output directory. */
  ]
}
