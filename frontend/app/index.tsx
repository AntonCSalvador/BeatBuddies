import { Text, View } from 'react-native';

export default function Index() {
    return (
        <View className="h-screen w-screen justify-center items-center bg-blue-500">
            <Text className="text-white text-lg">Loading...</Text>
        </View>
    );
}
